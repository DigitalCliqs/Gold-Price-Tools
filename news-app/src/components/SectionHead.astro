---
interface Props { title: string; href?: string; }
const { title, href } = Astro.props;
---
<div class="mag-secthead">
  <h2>{title}</h2>
  {href && <a class="mag-viewall" href={href}>View all &rarr;</a>}
</div>
