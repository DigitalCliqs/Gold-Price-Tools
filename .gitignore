pr.py
commit_helper.py
submit_pr.py
submit_helper.py
issue_body.txt
*.helper.py

# Python cache
__pycache__/
*.pyc
*.pyo

# Node
node_modules/

# AI Skills (installed per-machine)
.agent/
.agents/

# Stitch MCP — local config holds the real API key (use .mcp.json.example as template)
.mcp.json

# Google API credentials (sensitive - never commit)
client_secret*.json
gsc_token.json

# MCP server binaries (downloaded per-machine)
tools/psi-mcp/

# Playwright audit output (regenerable screenshots/strips)
tmp/

# Local SEO/analytics audit scripts — contain hardcoded API keys, keep local only
scripts/audit_bing.py
scripts/audit_clarity.py
scripts/audit_ga.py
scripts/audit_gsc.py
scripts/audit_psi.py
scripts/clarity_comprehensive_audit.py
scripts/clarity_full_audit.py
scripts/full_bing_audit.py
scripts/fix_analytics_ads.py
scripts/inspect_urls.py
scripts/run_mcp_client.py

# Audit output / reports (regenerable, may contain account data)
scripts/clarity_audit_results.json
reports/bing_tools.json
reports/ahref-issues-june/

# Local scratch work
scratch/

# Local API keys (Unsplash / Pexels image fetch) — never commit
.env
.env.local
.env.*.local

# Astro news-app build artifacts
news-app/node_modules
news-app/dist
news-app/.astro
